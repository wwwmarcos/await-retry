{
  "name": "await-retry",
  "version": "1.0.2",
  "description": "# install `npm i await-retry --save`",
  "main": "dist.js",
  "scripts": {
    "prepublish": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rollup index.js --format umd --name \"await-retry\" --file dist.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/eptaccio/await-retry.git"
  },
  "keywords": [
    "promise",
    "retry",
    "promise-tentatives"
  ],
  "author": "@eptaccio",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/eptaccio/await-retry/issues"
  },
  "homepage": "https://github.com/eptaccio/await-retry#readme",
  "devDependencies": {
    "rollup": "^0.64.1"
  }
}
